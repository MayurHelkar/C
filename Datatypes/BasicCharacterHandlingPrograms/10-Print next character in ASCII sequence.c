/*
Print next character in ASCII sequence

Display the next ASCII value’s character (e.g., A → B).

ASCII arithmetic, char increment
*/

#include <stdio.h>

int main()
{
    char Char = 'A';

    printf("Char next ASCII value : \'%c\'\n", Char + 1);

    return 0;
}